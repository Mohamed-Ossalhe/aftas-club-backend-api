package ma.youcode.aftasclubbackendapi.utils;

public class Utils {
    public static String generateCode(String location) {
        String locationCode = location.substring(0, 3);
        return locationCode;
//        String dateSub = date.substring(0);
    }
}
